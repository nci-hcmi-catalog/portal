const config = {
  urls: {
    cmsBase: import.meta.env.REACT_APP_CMS_API_URL,
    logoutUrl: import.meta.env.REACT_APP_LOGOUT_URL,
  },
  googleAppId: import.meta.env.REACT_APP_GOOGLE_CLIENT_ID,
};

export default config;
